/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com, plus.unsplash.com', 'chitchat-react.vercel.app', "res.cloudinary.com"]
    }
}

module.exports = nextConfig
