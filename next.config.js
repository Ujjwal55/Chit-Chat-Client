/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com, plus.unsplash.com', 'chitchat-react.vercel.app']
    }
}

module.exports = nextConfig
